{
  "name": "stream-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "tsc -p tsconfig.client.json",
    "build": "npm run build:server && npm run build:client && node scripts/copy-static.js",
    "start": "node dist/server/index.js"
  }
}
